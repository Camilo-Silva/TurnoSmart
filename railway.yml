# Configuración para Railway

build:
  type: dockerfile

deploy:
  startCommand: dotnet turno-smart.dll
  healthcheckPath: /

# Variables de entorno necesarias:
# ASPNETCORE_ENVIRONMENT=Production  
# DATABASE_URL=(proporcionado automáticamente por Railway)
